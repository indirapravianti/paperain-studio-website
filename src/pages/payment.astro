---
import Layout from '../layouts/Layout.astro';
---

<Layout title="payment">
  <section class="max-w-3xl mx-auto px-4 sm:px-6 py-10">
    <div id="payment-error" class="hidden text-center py-20">
      <div class="w-16 h-16 bg-red-50 rounded-full flex items-center justify-center mx-auto mb-4">
        <svg class="w-8 h-8 text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 16.5c-.77.833.192 2.5 1.732 2.5z" />
        </svg>
      </div>
      <h2 class="text-xl font-semibold mb-2">order not found</h2>
      <p class="text-gray-500 text-sm mb-6">please complete checkout first, or start a new order.</p>
      <a href="/products" class="inline-block text-sm border border-gray-200 rounded-full px-6 py-2 text-gray-600 hover:border-gray-400 transition-colors">
        back to shop
      </a>
    </div>

    <div id="payment-content" class="hidden">
      <div class="flex items-center gap-2 mb-8">
        <a href="/" class="text-gray-400 hover:text-primary transition-colors">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
          </svg>
        </a>
        <h1 class="text-2xl font-semibold">payment</h1>
      </div>

      <div class="grid md:grid-cols-5 gap-8">
        <div class="md:col-span-3">
          <div class="bg-pastel-blue/15 border border-pastel-blue/30 rounded-lg p-4 mb-6">
            <div class="flex items-center justify-between">
              <div>
                <p class="text-xs text-gray-400">order</p>
                <p id="pay-order-id" class="text-sm font-medium"></p>
              </div>
              <span class="text-xs px-3 py-1.5 rounded-full font-medium bg-yellow-100 text-yellow-700">awaiting payment</span>
            </div>
          </div>

          <div class="bg-buttermilk/40 border border-buttermilk rounded-lg p-4 mb-6">
            <p class="text-sm text-gray-600">
              <span class="font-medium">payment gateway coming soon.</span>
              we're waiting for our payment processor to approve our application. your order has been saved — we'll email you payment instructions shortly.
            </p>
          </div>

          <div class="mb-6">
            <h2 class="font-medium mb-4">select payment method</h2>
            <p class="text-xs text-gray-400 mb-3">preview only — payment is not active yet</p>

            <div class="space-y-3 opacity-60 pointer-events-none" id="payment-methods">
              <label class="flex items-center gap-4 border border-gray-200 rounded-xl p-4 has-[:checked]:border-primary has-[:checked]:bg-primary/5">
                <input type="radio" name="payment-method" value="credit_card" class="accent-primary w-4 h-4" checked disabled />
                <div class="flex items-center gap-3 flex-1">
                  <div class="w-10 h-10 bg-gray-50 rounded-lg flex items-center justify-center">
                    <svg class="w-5 h-5 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z" />
                    </svg>
                  </div>
                  <div>
                    <p class="text-sm font-medium">credit / debit card</p>
                    <p class="text-xs text-gray-400">visa, mastercard, jcb</p>
                  </div>
                </div>
              </label>

              <label class="flex items-center gap-4 border border-gray-200 rounded-xl p-4">
                <input type="radio" name="payment-method" value="bank_transfer" class="accent-primary w-4 h-4" disabled />
                <div class="flex items-center gap-3 flex-1">
                  <div class="w-10 h-10 bg-gray-50 rounded-lg flex items-center justify-center">
                    <svg class="w-5 h-5 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
                    </svg>
                  </div>
                  <div>
                    <p class="text-sm font-medium">bank transfer</p>
                    <p class="text-xs text-gray-400">bca, bni, bri, mandiri, permata</p>
                  </div>
                </div>
              </label>

              <label class="flex items-center gap-4 border border-gray-200 rounded-xl p-4">
                <input type="radio" name="payment-method" value="e_wallet" class="accent-primary w-4 h-4" disabled />
                <div class="flex items-center gap-3 flex-1">
                  <div class="w-10 h-10 bg-gray-50 rounded-lg flex items-center justify-center">
                    <svg class="w-5 h-5 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z" />
                    </svg>
                  </div>
                  <div>
                    <p class="text-sm font-medium">e-wallet</p>
                    <p class="text-xs text-gray-400">gopay, shopeepay, dana, ovo</p>
                  </div>
                </div>
              </label>

              <label class="flex items-center gap-4 border border-gray-200 rounded-xl p-4">
                <input type="radio" name="payment-method" value="qris" class="accent-primary w-4 h-4" disabled />
                <div class="flex items-center gap-3 flex-1">
                  <div class="w-10 h-10 bg-gray-50 rounded-lg flex items-center justify-center">
                    <svg class="w-5 h-5 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 4v1m6 11h2m-6 0h-2v4m0-11v3m0 0h.01M12 12h4.01M16 20h4M4 12h4m12 0h.01M5 8h2a1 1 0 001-1V5a1 1 0 00-1-1H5a1 1 0 00-1 1v2a1 1 0 001 1zm12 0h2a1 1 0 001-1V5a1 1 0 00-1-1h-2a1 1 0 00-1 1v2a1 1 0 001 1zM5 20h2a1 1 0 001-1v-2a1 1 0 00-1-1H5a1 1 0 00-1 1v2a1 1 0 001 1z" />
                    </svg>
                  </div>
                  <div>
                    <p class="text-sm font-medium">qris</p>
                    <p class="text-xs text-gray-400">scan with any e-wallet or banking app</p>
                  </div>
                </div>
              </label>
            </div>
          </div>

          <button id="pay-btn" disabled
            class="w-full bg-primary/50 text-white py-3.5 rounded-lg text-sm font-medium cursor-not-allowed">
            pay now — coming soon
          </button>
          <p class="text-xs text-gray-400 text-center mt-3">
            secured by <span class="font-medium text-gray-500">midtrans</span> payment gateway
          </p>
        </div>

        <div class="md:col-span-2">
          <div class="bg-gray-50 rounded-xl p-6 sticky top-24">
            <h2 class="font-medium mb-4">order summary</h2>
            <div id="pay-items" class="space-y-3 mb-4"></div>

            <div class="border-t border-gray-200 pt-4 space-y-2">
              <div class="flex justify-between text-sm">
                <span class="text-gray-500">subtotal</span>
                <span id="pay-subtotal" class="text-gray-600"></span>
              </div>
              <div id="pay-discount-row" class="flex justify-between text-sm text-green-600 hidden">
                <span>discount (20%)</span>
                <span id="pay-discount"></span>
              </div>
              <div id="pay-promo-row" class="flex justify-between text-sm text-green-600 hidden">
                <span id="pay-promo-label">promo</span>
                <span id="pay-promo-discount"></span>
              </div>
              <div class="flex justify-between text-sm">
                <span class="text-gray-500">shipping</span>
                <span id="pay-shipping" class="text-gray-600"></span>
              </div>
              <div class="flex justify-between font-medium border-t border-gray-200 pt-2">
                <span>total</span>
                <span id="pay-total"></span>
              </div>
            </div>

            <div class="border-t border-gray-200 pt-4 mt-4">
              <p class="text-xs text-gray-400 mb-2">shipping to</p>
              <p id="pay-ship-name" class="text-sm font-medium"></p>
              <p id="pay-ship-email" class="text-sm text-gray-500 mt-0.5"></p>
              <p id="pay-ship-address" class="text-sm text-gray-500 mt-0.5 whitespace-pre-line"></p>
            </div>
          </div>
        </div>
      </div>

      <div class="mt-8 text-center">
        <a href="/" class="inline-block text-sm border border-gray-200 rounded-full px-6 py-2 text-gray-600 hover:border-gray-400 transition-colors">
          continue shopping
        </a>
      </div>
    </div>
  </section>
</Layout>

<script is:inline>
  (function() {
    var params = new URLSearchParams(window.location.search);
    var orderId = params.get('id');

    var data = null;
    try {
      data = JSON.parse(sessionStorage.getItem('paperain-pending-payment'));
    } catch {}

    if (!data) {
      document.getElementById('payment-error').classList.remove('hidden');
      return;
    }

    if (orderId && data.order && data.order.id !== orderId) {
      document.getElementById('payment-error').classList.remove('hidden');
      return;
    }

    function fmtDual(usd, idr) {
      return '$' + Number(usd).toFixed(2) + ' / Rp' + Math.round(idr).toLocaleString('id-ID');
    }

    document.getElementById('payment-content').classList.remove('hidden');
    document.getElementById('pay-order-id').textContent = data.order.display_id || data.order.id || '';

    var itemsEl = document.getElementById('pay-items');
    itemsEl.innerHTML = (data.items || []).map(function(item) {
      var usd = (item.priceUsd || item.price || 0) * item.qty;
      var idr = (item.priceIdr || 0) * item.qty;
      return '<div class="flex justify-between text-sm">' +
        '<span class="text-gray-600">' + item.title + ' &times; ' + item.qty + '</span>' +
        '<span>' + fmtDual(usd, idr) + '</span></div>';
    }).join('') || '<p class="text-sm text-gray-400">no items</p>';

    var s = data.summary || {};
    document.getElementById('pay-subtotal').textContent = fmtDual(s.subtotalUsd || 0, s.subtotalIdr || 0);

    if ((s.volumeDiscountUsd || 0) > 0 || (s.volumeDiscountIdr || 0) > 0) {
      document.getElementById('pay-discount-row').classList.remove('hidden');
      document.getElementById('pay-discount').textContent = '-' + fmtDual(s.volumeDiscountUsd || 0, s.volumeDiscountIdr || 0);
    }
    if ((s.promoDiscount || 0) > 0) {
      document.getElementById('pay-promo-row').classList.remove('hidden');
      document.getElementById('pay-promo-label').textContent = s.promoCode ? 'promo (' + s.promoCode + ')' : 'promo';
      document.getElementById('pay-promo-discount').textContent = '-$' + Number(s.promoDiscount).toFixed(2);
    }

    document.getElementById('pay-shipping').textContent = fmtDual(s.shippingUsd || 0, s.shippingIdr || 0);
    document.getElementById('pay-total').textContent = fmtDual(s.totalUsd || 0, s.totalIdr || 0);

    if (data.shipping) {
      document.getElementById('pay-ship-name').textContent = data.shipping.name || '';
      document.getElementById('pay-ship-email').textContent = data.shipping.email || '';
      document.getElementById('pay-ship-address').textContent = data.shipping.address || '';
    }
  })();
</script>
